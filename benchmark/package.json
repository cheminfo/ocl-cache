{
  "name": "benchmark",
  "version": "1.0.0",
  "private": true,
  "main": "benchmark.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "p-all": "^3.0.0"
  }
}
